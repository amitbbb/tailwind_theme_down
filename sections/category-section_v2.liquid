    <div class="flex flex-wrap justify-between text-center bg-[#E2E4E2] py-[40px] pb-[60px] me:py-[50px] me:px-[40px] lg:py-[50px] lg:px-[70px] xl:px-[90px] xl:py-[90px]">
    {% for block in section.blocks %}
        {% if block.settings.image != blank %}
      <div class="w-full me:w-1/3 px-[15px] me:px-[25px] mb-[20px] {% if forloop.index == 2 %}me:flex me:flex-col-reverse me:justify-between{% endif %}">
        <div class="p-[8%] me:p-0"> 
        {% assign img_url = block.settings.image | img_url: "master" %}
           <img class="lazyload w-full h-auto" 
           src="{{ img_url }}"
           data-src="{{ img_url }}"
           data-widths="[180, 360, 470, 600, 770, 970, 1060, 1280, 1512, 1728, 2048]"
           data-aspectratio="{{ img.aspect_ratio }}"
           data-sizes="auto" width="100" height="100">
           </div>
        <div class="{% if forloop.index == 2 %}mb-[10px]{% endif %}"> 
            <h2 class="text-[34px] leading-[44px] lg:text-[45px] lg:leading-[62px] lg:mt-[20px] font-[Compacta] {% if forloop.index == 2 %}lg:mt-0{% endif %}">{{ block.settings.heading }}</h2>
            <p class="text-[18px] leading-[24px] lg:text-[22px] lg:leading-[26px] my-[15px] font-[Kepler] h-[60px] me:h-[100px] flex justify-center items-center">{{ block.settings.text }}</p>
            <p class="me:my-[15px]"><a class="border border-black border-solid font-[Tungsten-Book] text-[32px] leading-normal py-[10px] px-[30px]" href="{{ block.settings.url }}">Shop Now</a></p>
        </div>
      </div>
        {% endif %}
    {% endfor %}
    </div>
  {% schema %}
  {
    "name": "Categories V2",
    "settings": [
        {
          "type": "text",
          "id": "heading",
          "label":"Main Heading",
          "default": "New Colors In Planters, Coasters And Everyone's Favorite Bookends Are Here"
        }
    ],
    "blocks": [
      {
        "type": "Category",
        "name": "Category Block",
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label":"Category Image"
          },
          {
            "type": "text",
            "id": "heading",
            "label":"Heading"
          },
          {
            "type": "text",
            "id": "text",
            "label": "Small Description"
          },
          {
            "type": "url",
            "id": "url",
            "label":"Category URL"
          }
        ]
      }
    ],
    "presets" : [
      {
       "name": "Categories V2"
      }
    ]
  }
  {% endschema %}